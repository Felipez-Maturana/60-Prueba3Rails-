<div class="container">
  <h2>Lista de canciones de: <%= @user.name %> </h2>             
  <table class="table">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Duracion</th>
        <th>Género</th>
      </tr>
    </thead>
    <tbody>
    <% @canciones.each do |x| %>
      <tr>
        <td><%= x.name %></td>
        <td><%= x.duration %></td>
        <td><%= x.genre %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>